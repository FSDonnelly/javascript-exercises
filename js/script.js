// Functions witn Arguments
function square(x) {
  let result = x * x;
  console.log(result);
}

square(3);
square(12);

function area(length, width) {
  let area = length * width;
  console.log(area);
}

area(20, 10);
area(30, 20);
